# Codex 工作说明

## 项目目标

构建个人使用的市场监控、投研摘要和风险控制系统。系统只提供信息与风险提示，不自动下单，不输出未经证据支持的确定性涨跌预测。

## 必须遵守

- 修改前先阅读 `README.md`。
- 保持 LLM 提供商可替换，不得把 OpenAI 或 DeepSeek 调用散落到业务代码。
- 数字计算、仓位限制和交易规则必须由确定性代码完成。
- LLM 输出必须经过 Pydantic 校验。
- 每条事实必须带来源和数据时间。
- 不得在日志中输出 API Key、持仓账户、Cookie 或访问令牌。
- 不得加入券商自动下单功能。
- 不得执行真实交易。
- 新功能必须包含测试。
- 不得静默吞掉数据源错误；数据过期时必须明确降级或拒绝生成结论。

## 首个开发任务建议

1. 运行现有测试并理解架构；
2. 新增 `TradingCalendar` 接口；
3. 为开发环境实现 `WeekdayCalendar`；
4. 为生产环境预留真实交易日历适配器；
5. 所有定时任务执行前检查交易日；
6. 添加节假日跳过测试；
7. 不接入任何真实券商账户。
